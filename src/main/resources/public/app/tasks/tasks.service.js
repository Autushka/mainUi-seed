'use strict';

var tasksModule = require('./_index.js');

function tasksService($q, $http, Upload, $cookies, APP_SETTINGS, globalService) {
    var apiUrl = APP_SETTINGS.apiUrl;
    var service = {};

    return service;
}

tasksModule.service('tasksService', tasksService);